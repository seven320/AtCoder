# encoding:utf-8
import copy
import numpy as np
import random
import bisect #bisect_left　これで二部探索の大小検索が行える
import math


S = str(input())
T = str(input())

count = 0
replace_S = []


# print(replace_S)
def check(i,j):
    if
        if  ==  or
        should_match -= 1

    return ans

ans = ""
for i in range(len(S)):
    for j in range(len(T)):
        if S[i] == T[j]:
            ans = check(i,j)
